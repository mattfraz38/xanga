<h1>All Posts</h1>

<ol>
  <% @posts.each do |post| %>
      <li>
        <p><%= link_to post.title, post_path(post) %></p>
        <small><%= link_to post.user.username, user_path(post) %></small>
      </li>
  <% end %>
</ol>

<%= link_to 'New Post', new_post_path %>
